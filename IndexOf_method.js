//This actually actually extract the index of an array which we are simply searching for.
//Suppose there are five elements in the original array and then we want the indexo of third element then
//in that case we use this indexOf method.
// it's illustration is shown below.

const seat = ["4E", "5B", "6C", "7D", "8E", "9A"];

const newSeat = seat.indexOf("6C");

console.log(newSeat);
